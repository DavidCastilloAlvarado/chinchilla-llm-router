# Secrets — never commit real credentials.
.env

# Build artifacts (root bin/ is the Go build output; installer/bin/ is source)
/bin/
dist/
*.exe

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/

# local session exports
pi-session-*.html
